import { Metadata } from "next";

export const metadata: Metadata = {
  title: "GitHub Profile Finder",
  description: "App to search GitHub profile",
};

export default function Home() {
  return <></>;
}
